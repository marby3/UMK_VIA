# All build output goes here
/build/

# Flashable .bin + definition JSON sets handed out for testing
/dist/

__pycache__/
*.pyc

# ch32fun.mk writes this literal file when its Windows NULLDEV meets a POSIX shell
firmware/nul

# VS Code Local History extension
.history/

# AI review runs: keep the reviewer config, not the per-run prompts and reports
.review/*
!.review/config
